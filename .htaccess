# ============================================================
# BSU-Bokod SIMS - Apache Configuration
# ============================================================

# ----------------------------------------------------------
# 1. SECURITY: Disable Directory Browsing
# ----------------------------------------------------------
Options -Indexes

# ----------------------------------------------------------
# 2. SECURITY: Block Access to Sensitive Files
# ----------------------------------------------------------
<FilesMatch "\.(htaccess|htpasswd|ini|log|sql|md|env|git|gitignore)$">
    Require all denied
</FilesMatch>

# Protect specific directories
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(include|database_migrations|admin/back-up)/ - [F,L]
</IfModule>

# ----------------------------------------------------------
# 3. CLEAN URLs: Remove .php Extension (Optional)
# ----------------------------------------------------------
# Uncomment to enable clean URLs (index.php becomes /index)

# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteBase /SIMS/
#     
#     # Remove .php extension
#     RewriteCond %{REQUEST_FILENAME} !-d
#     RewriteCond %{REQUEST_FILENAME}.php -f
#     RewriteRule ^(.+?)/?$ $1.php [L]
#     
#     # Redirect .php to clean URL
#     RewriteCond %{THE_REQUEST} ^GET\ (.*)\.php(\?.*)?\ HTTP
#     RewriteRule (.*)\.php$ $1 [R=301,L]
# </IfModule>

# ----------------------------------------------------------
# 4. SECURITY HEADERS
# ----------------------------------------------------------
<IfModule mod_headers.c>
    # Prevent clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Prevent MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Enable XSS protection
    Header always set X-XSS-Protection "1; mode=block"
    
    # Referrer policy
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Remove server signature
    Header always unset X-Powered-By
    Header always unset Server
</IfModule>

# ----------------------------------------------------------
# 5. PHP SETTINGS (If allowed)
# ----------------------------------------------------------
<IfModule mod_php7.c>
    # Hide PHP version
    php_flag expose_php off
    
    # Disable display_errors in production (enable for development)
    # php_flag display_errors off
    # php_flag display_startup_errors off
    
    # Set upload limits
    php_value upload_max_filesize 5M
    php_value post_max_size 5M
    
    # Session security
    php_flag session.cookie_httponly on
    php_flag session.use_only_cookies on
</IfModule>

# ----------------------------------------------------------
# 6. ERROR PAGES (Optional)
# ----------------------------------------------------------
# Uncomment to use custom error pages
# ErrorDocument 404 /SIMS/error404.php
# ErrorDocument 403 /SIMS/error403.php
# ErrorDocument 500 /SIMS/error500.php

# ----------------------------------------------------------
# 7. COMPRESSION (Better Performance)
# ----------------------------------------------------------
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# ----------------------------------------------------------
# 8. BROWSER CACHING (Better Performance)
# ----------------------------------------------------------
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/pdf "access plus 1 month"
    ExpiresByType text/x-javascript "access plus 1 month"
</IfModule>

# ----------------------------------------------------------
# 9. DEFAULT DOCUMENT
# ----------------------------------------------------------
DirectoryIndex index.php index.html

# ----------------------------------------------------------
# 10. FORCE HTTPS (Uncomment when you have SSL)
# ----------------------------------------------------------
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteCond %{HTTPS} off
#     RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
# </IfModule>
